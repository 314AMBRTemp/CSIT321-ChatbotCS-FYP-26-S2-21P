# The Flask adapter (backend/services/rasa_adapter.py) talks to this channel.
# POST http://localhost:5005/webhooks/rest/webhook
#   {"sender": "<employeeId>", "message": "<question>"}
#
# sender is the HRMS employee id — that is how the custom actions know which
# employee they are acting for (see actions/actions.py -> _employee_id).
rest:

# Enable only if you also want to poke the bot from a terminal:
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: false
